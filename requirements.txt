# ---- Apple Silicon (M1/M2/M3 8GB+) MLX stack ----
# Install these via pip on macOS 14+
mlx>=0.20.0
mlx-lm>=0.22.0
# mflux is only needed if diffusion.model is schnell/dev
# mflux>=0.12.0
mlx-vlm>=0.2.0

# SDXL (default image backend) — much smaller than FLUX.1
diffusers>=0.30.0
accelerate>=0.33.0
peft>=0.11.0
controlnet-aux>=0.0.9
opencv-python-headless>=4.8.0

# ---- Torch (only needed for CUDA / optional CLIP ranker) ----
# On Apple Silicon torch is NOT required for the main pipeline (LLM+FLUX use MLX).
# Install only if you plan to run CUDA / transformers:
# torch>=2.3.0
# transformers>=4.43.0
# accelerate

# ---- Core framework ----
langgraph>=1.1.0
langchain>=0.3.0
langchain-core>=0.3.0
langsmith>=0.1.0

# ---- Utilities ----
pydantic>=2.7.0
pydantic-settings
PyYAML>=6.0
pillow>=10.0.0
numpy>=1.26.0
requests>=2.31.0
tqdm>=4.66.0
python-dotenv>=1.0.0
httpx>=0.27.0
typer>=0.12.0
rich>=13.0.0
